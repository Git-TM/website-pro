
<div id="modal-window-<%=project.id%>" class="modal" role="dialog" aria-labelledby="show" aria-hidden="true">
  <div class="modal-content" role="document">

    <div class="modal-title text-center mb-1">
      <%= submit_tag 'X', :type => :reset, :class => "close-modal", "data-dismiss" => "modal", "aria-hidden" => "true" %>
      <h2 class="gradient-text"><%= project.projecttype %></h2>
      <span><%= project.context %></span>
    </div>

    <div class="modal-wrapper d-flex">

      <div class="modal-photos-div">
        <img src=<%= image_path("mokup-tracking.jpeg")%> class="photo-modal" />
        <img src=<%= image_path("mokup-website.jpeg")%> class="photo-modal" />
      </div>

      <div class="modal-desc">
        <h2 class="text-left"> Description</h2>
        <p><%= project.context %></p>
        <h2 class="text-left"> Fonctionnalités</h2>
        <ul>
        <% project.features.split(',').map(&:strip).each do |feature| %>
        <li><%= feature %></li>
        <% end %>
        </ul>
        <h2> General tags </h2>
        <div class="codetags-wrapper d-flex">
          <% project.codetags.split(',').map(&:strip).each do |tag| %>
          <div class="techno">
            <span class="techno-desc"><%= tag %></span>
          </div>
          <% end %>
        </div>

      </div>


    </div>

  </div>
</div>
